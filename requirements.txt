fastapi~=0.121.1
pydantic~=2.10.6
starlette~=0.50.0
uvicorn~=0.38.0
colorama~=0.4.6             # ANSI-цвета логов в Windows-консоли (main.py)
psutil~=7.0.0               # LocalIPResolver: TCP-таблица + интерфейсы
pyinstaller
websockets
msgpack~=1.2.1             # binary wire-формат (MsgPack → msgpack frames)
pyyaml~=6.0.2              # YAML configuration
cryptography~=46.0.7       # SSL/TLS certificate generation (sign/signer.py)
watchdog~=6.0.0            # hot-reload сервисов

# EyeSauron (services/eyesauron): захват экрана + дедуп-хранилище (vendor)
mss~=10.0.0                # быстрый захват экрана (хелпер сессии)
pillow~=11.0.0             # PNG-кодирование, avg_hash
numpy~=2.2.0               # avg_hash + vendor ChunkStore

# Web panel
streamlit~=1.54.0      # Streamlit dashboard
pandas~=2.2.3         # DataFrames for web UI
streamlit-agraph   # интерактивный граф карты сети (только WebUI-сборка)
streamlit-ace==0.1.1  # YAML-подсветка редактора config (fallback когда code_editor недоступен)
