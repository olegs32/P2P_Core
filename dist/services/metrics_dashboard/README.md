# Metrics Dashboard Service

Веб-интерфейс для мониторинга и управления P2P кластером.

## Описание

Сервис `metrics_dashboard` предоставляет веб-интерфейс для:
- Мониторинга метрик координатора и воркеров
- Управления сертификатами в кластере
- Просмотра информации о сервисах
- Деплоя сертификатов между узлами

## Архитектура

### Компоненты

1. **Backend (main.py)**
   - FastAPI сервер на порту 8000
   - Сбор метрик от metrics_reporter
   - API endpoints для управления сертификатами
   - Хранение данных в памяти

2. **Frontend (templates/dashboard.html)**
   - Табированный интерфейс с боковым меню
   - Bootstrap 5 для стилей
   - Chart.js для визуализации метрик
   - Адаптивный темный дизайн

### Система сбора данных

Данные собираются через сервис `metrics_reporter`, работающий на каждом воркере:
1. `metrics_reporter` собирает метрики и данные сервисов
2. Отправляет данные на координатор через RPC
3. `metrics_dashboard` хранит и отображает полученные данные

## Функциональность

### Вкладка "Главная" (Home)

**Мониторинг координатора:**
- CPU, Memory, Disk, Network в реальном времени
- График истории метрик (последние 50 точек)
- Обновление каждые 5 секунд

**Список воркеров:**
- Статус подключения (Online/Offline)
- Основные метрики: CPU, Memory, Disk
- Количество активных сервисов
- Кнопка просмотра деталей

**Все сервисы кластера:**
- Модальное окно со списком всех сервисов
- Группировка по узлам (координатор + воркеры)
- Информация о статусе каждого сервиса

### Вкладка "Сертификаты" (Certificates)

#### 1. Установка сертификатов

**Форма загрузки:**
- Выбор целевого узла (координатор или воркер)
- Загрузка PFX файла
- Ввод пароля для PFX
- Автоматическая установка сертификата

**Процесс установки:**
1. Загрузка PFX файла в браузере
2. Кодирование в base64
3. Отправка через RPC на целевой узел
4. Установка через `certs_tool` сервис
5. Автоматическое обновление списка

#### 2. Просмотр сертификатов

**Фильтрация:**
- По узлам: All Nodes, Coordinator, Worker1, Worker2...
- Динамическое обновление списка узлов
- Счетчик отфильтрованных сертификатов

**Таблица сертификатов:**
- Node - узел расположения
- Subject - владелец сертификата (CN)
- Issuer - издатель сертификата (CN)
- Valid From - дата начала действия
- Valid To - дата окончания действия
- Status - статус с цветовой индикацией:
  - 🟢 Valid - действителен
  - 🟡 Expiring in X days - истекает (< 30 дней)
  - 🔴 Expired - истек
  - ⚪ Unknown - неизвестен

#### 3. Управление сертификатами

**Кнопка View:**
- Модальное окно с полной информацией
- Subject (CN и полный DN)
- Issuer (CN и полный DN)
- Даты действия
- Serial Number (серийный номер)
- Thumbprint (SHA1 отпечаток)
- Container (имя контейнера закрытого ключа)

**Кнопка CER:**
- Экспорт публичной части сертификата
- Скачивание в формате .cer
- Для передачи публичного ключа

**Кнопка PFX:**
- Экспорт с закрытым ключом
- Запрос пароля для защиты
- Скачивание в формате .pfx
- Для резервного копирования или переноса

**Кнопка Deploy:**
- Отображается только для сертификатов координатора
- Показывается только если есть воркеры БЕЗ этого сертификата
- Сравнение по серийному номеру
- Выбор целевого воркера из списка узлов без сертификата
- Ввод пароля для защиты при передаче
- Автоматический деплой: экспорт → передача → установка

**Кнопка Del:**
- Удаление сертификата из системы
- Подтверждение операции
- Удаление по thumbprint через `certmgr.exe`

#### 4. Деплой сертификатов

**Умная логика отображения Deploy:**
```javascript
// Deploy показывается только если:
1. Сертификат находится на координаторе
2. Есть воркеры БЕЗ этого сертификата
3. Сравнение идет по serial number
```

**Процесс деплоя:**
1. Экспорт сертификата с координатора в PFX (с закрытым ключом)
2. Кодирование в base64 для безопасной передачи
3. Передача через RPC на целевой воркер
4. Установка на воркере
5. Автоматическая очистка временных файлов

**Модальное окно деплоя:**
- Название сертификата
- Список только воркеров БЕЗ этого сертификата
- Поле пароля для защиты PFX
- Статус операции в реальном времени

## API Endpoints

### Метрики

**GET /** - Главная страница дашборда

**POST /api/dashboard/metrics** - Прием метрик от воркеров
```json
{
  "worker_id": "worker1",
  "metrics": {
    "cpu": 45.2,
    "memory": 60.5,
    "disk": 70.0,
    "network": 1024
  },
  "services": {
    "service1": {"status": "running"},
    "service2": {"status": "stopped"}
  },
  "service_data": {
    "certs_tool": {
      "service_type": "certificates",
      "certificates": [...]
    }
  }
}
```

**GET /api/dashboard/data** - Получение всех метрик
```json
{
  "coordinator": {
    "cpu": 30.0,
    "memory": 50.0,
    ...
  },
  "workers": {
    "worker1": {...},
    "worker2": {...}
  }
}
```

**GET /api/dashboard/service-data?service_type=certificates** - Данные сервисов
```json
{
  "timestamp": "2025-01-13T12:00:00",
  "certificates": [
    {
      "worker": "coordinator",
      "id": "0_John Doe",
      "subject_cn": "John Doe",
      "issuer_cn": "CA Authority",
      "serial": "0x00C9A7A415...",
      "thumbprint": "A1B2C3D4...",
      "valid_from": "03/07/2025 05:27:49 UTC",
      "valid_to": "26/09/2026 05:27:49 UTC"
    }
  ],
  "total": 1
}
```

### Сертификаты

**POST /api/certificates/install** - Установка сертификата
```json
{
  "worker": "coordinator",
  "pfx_data": "base64_encoded_pfx",
  "password": "password",
  "filename": "cert.pfx"
}
```

**GET /api/certificates/export-cer?worker=coordinator&cert_id=0_Name** - Экспорт CER
- Возвращает файл .cer

**GET /api/certificates/export-pfx?worker=coordinator&cert_id=0_Name&password=pass** - Экспорт PFX
- Возвращает файл .pfx

**POST /api/certificates/delete** - Удаление сертификата
```json
{
  "worker": "coordinator",
  "cert_id": "0_John Doe"
}
```

**POST /api/certificates/deploy** - Деплой сертификата
```json
{
  "cert_id": "0_John Doe",
  "source_worker": "coordinator",
  "target_worker": "worker1",
  "password": "password"
}
```

## Интеграция с сервисами

### metrics_reporter

Для передачи данных в дашборд сервис должен реализовать метод `get_dashboard_data()`:

```python
@service_method(description="Get dashboard data", public=True)
async def get_dashboard_data(self) -> Dict[str, Any]:
    return {
        "service_name": "My Service",
        "service_type": "custom",  # certificates, metrics, etc.
        "data": {
            # Произвольные данные для отображения
        }
    }
```

Этот метод будет автоматически вызван `metrics_reporter` и данные отправлены на дашборд.

### certs_tool

Интеграция для управления сертификатами:

**Методы:**
- `list_certificates()` - список всех сертификатов
- `get_dashboard_data()` - данные для дашборда
- `export_certificate_cer()` - экспорт в CER
- `export_certificate_pfx()` - экспорт в PFX
- `install_pfx_from_base64()` - установка из base64
- `delete_certificate()` - удаление по thumbprint

## Конфигурация

### Порты
- **8000** - HTTP сервер дашборда

### Зависимости
- FastAPI
- Uvicorn
- Bootstrap 5 (CDN)
- Chart.js (CDN)

### Переменные окружения
Используются настройки из основного конфига кластера.

## Безопасность

### Передача данных
- Все операции через RPC proxy координатора
- Нет прямого доступа к воркерам из браузера
- PFX файлы защищены паролем при передаче

### Сертификаты
- Временные файлы автоматически удаляются
- Пароли не логируются
- Thumbprint для идентификации
- Serial number для сравнения

### Валидация
- Проверка наличия обязательных полей
- Валидация типов данных
- Обработка ошибок с информативными сообщениями

## Примеры использования

### Установка сертификата на координатор
1. Открыть вкладку "Сертификаты"
2. Выбрать "Coordinator" в Target Node
3. Загрузить PFX файл
4. Ввести пароль
5. Нажать "Install Certificate"

### Деплой сертификата на воркер
1. Найти сертификат координатора в списке
2. Нажать кнопку "Deploy" (если доступна)
3. Выбрать целевой воркер
4. Ввести пароль для защиты при передаче
5. Нажать "Deploy Certificate"

### Экспорт для резервной копии
1. Найти нужный сертификат
2. Нажать "PFX"
3. Ввести пароль для защиты файла
4. Сохранить скачанный .pfx файл

### Просмотр деталей
1. Нажать "View" у сертификата
2. Изучить полную информацию
3. Проверить даты действия и статус

## Разработка

### Структура файлов
```
metrics_dashboard/
├── __init__.py
├── main.py              # Backend FastAPI
├── README.md            # Документация
└── templates/
    └── dashboard.html   # Frontend интерфейс
```

### Добавление новой вкладки

1. **HTML (dashboard.html):**
```html
<!-- Добавить в меню -->
<div class="nav-tab" data-tab="mytab">
    <i class="bi bi-icon"></i>
    <span>My Tab</span>
</div>

<!-- Добавить контент -->
<div id="mytab" class="tab-content">
    <!-- Содержимое вкладки -->
</div>
```

2. **JavaScript:**
```javascript
// Обработка загрузки данных
async function loadMyData() {
    const response = await fetch('/api/my-data');
    const data = await response.json();
    // Отобразить данные
}
```

3. **Backend (main.py):**
```python
@app.get("/api/my-data")
async def get_my_data():
    return {"data": "..."}
```

### Стили и темизация

Используются CSS переменные для темной темы:
```css
--bg-primary: #1a1a2e
--bg-secondary: #16213e
--text-primary: #e0e0e0
--accent: #0f3460
```

## Troubleshooting

### Сертификаты не отображаются
- Проверить работу `certs_tool` сервиса
- Убедиться что `metrics_reporter` запущен на узле
- Проверить логи на наличие ошибок парсинга

### Deploy не работает
- Убедиться что сертификат находится на координаторе
- Проверить что serial number заполнен
- Убедиться что воркер онлайн

### Метрики не обновляются
- Проверить работу `metrics_reporter` на воркерах
- Убедиться в связности с координатором
- Проверить интервал отправки метрик (default 5 сек)

## Логирование

Логи содержат:
- Операции с сертификатами (установка, удаление, деплой)
- Ошибки при обработке запросов
- Статус деплоя сертификатов

Уровень логирования настраивается в конфигурации сервиса.

## Версия

Текущая версия: 2.0

### Changelog

**2.0** (2025-01-13)
- Табированный интерфейс
- Управление сертификатами
- Деплой между узлами
- Умная кнопка Deploy
- Детальный просмотр сертификатов
- Фильтрация по узлам

**1.0** (Initial)
- Базовый мониторинг метрик
- Список воркеров
- График координатора
